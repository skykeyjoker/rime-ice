# 2026-08-31 上游更新审查

## 基线

- 雾凇：`c398c0d4526b012cb3b306f792089abed13e0413` →
  `75e6572bebc05b49021e842949ce947882e3e4b2`；日期标签仍为 `2026.06.30`。
- 白霜参考：`6b74e54da2737d9b9ba8c502ce697145f94504ba` →
  `328ded2ac3978c0a2b56c18b9d7ec05dd8b18130`；稳定标签仍为 `1.0.4`。

## 已采用

- 雾凇 `8775c4c`：取消“休息 → ♨️”。
- 雾凇 `75e6572`：更新 `8105/base/ext/tencent`、Emoji 映射及构建纠错表。
  包括“省身”“大藏家”等纠音、词频调整和冗余词条清理。
- 补回 `others/emoji-map.txt` 的 `♨️ 温泉`：上游生成文件保留了“温泉”，
  但源文件整行删除，直接重新构建会造成生成树差异。仅恢复“温泉”源映射，
  不恢复“休息”映射；构建结果与期望生成文件一致。

## 未采用

- 白霜本轮只有主词库、符号及拆分文件变化，均超出次级参考范围。
- 两个白霜提交的 `cn_dicts_cell/` tree SHA 均为
  `d019d20a1834a31232d08db821a216bc2a789196`，无需更新细胞词库。
- 未改动 Lua、schema、自定义导入、万象配置、个人短语或前端样式。

## 本地验证

- 仓库 `build` 通过；生成文件没有额外差异。
- `lint` 通过：30 个 YAML、33 个 Lua；8 条警告来自未改动的
  `cold_word_drop/logger.lua` 与 `cold_word_drop/metatable.lua`，没有错误。
- 35 个导入词库路径全部存在；本机 Rime 1.17.0 编译得到 5,523,762 条记录。
- 隔离用户库验证：`xian` 首两项仍为本地“先、现”；长句同时有 `∞` 和云候选；
  选择前半句后查询剩余拼音，回退后恢复整段查询，继续编辑后云候选正常刷新。
  回退保留非首选高亮时，既有防跳词保护会暂缓刷新，未修改该行为。
- `fangjungen`、`uuid`、`cC1+1`、`uU` 注音/补充/`n/a` 均正常；
  `kmjkaixin` 为 6 个独立颜文字候选并取消云查询，普通拼音不混入颜文字。
- “休息”不再出现 ♨️，“温泉”仍保留 ♨️。
- 本机 Fcitx5 已更新 7 个词库/映射相关源文件并部署、重启；状态恢复为 `2 / rime`。
  模型、schema/custom、云 Lua、前端主题及两个拆字预编译文件的哈希保持不变；
  两个拆字预编译文件的 inode、大小和修改时间也未变化。
- 输入验证使用隔离用户库，未覆盖真实学习数据。Windows 小狼毫未执行真实端到端验证。
